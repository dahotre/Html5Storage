h1. Html5storage

This is a tiny Rails + Javascript + HTML 5 application to demo How-to use localStorage (or sessionStorage) to load content on a website instead of querying the server, thus resulting in snappier user experience.
